<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.facebookloginpage">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FacebookLoginPage">
        <activity android:name=".SQLiteDatabashHelper.EditContactViewActivity"></activity>
        <activity android:name=".SQLiteDatabashHelper.AddContactActivity" />
        <activity android:name=".SQLiteDatabashHelper.ContactManagerActivity">

        </activity>
        <activity android:name=".RoomDAO.EditContactView2Activity"></activity>
        <activity android:name=".RoomDAO.AddContact2Activity" />
        <activity android:name=".RoomDAO.ContactManager2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UsersInfoActivty" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>